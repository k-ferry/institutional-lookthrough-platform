# Backend

This folder will contain backend services and APIs.

Planned components:
- Python-based API layer (e.g., FastAPI)
- Deterministic tools used by the UI and workflow agents
- Logging and audit event writers

The backend is designed to be modular and reproducible.
